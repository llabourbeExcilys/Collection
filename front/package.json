{
  "name": "front",
  "version": "1.0.0",
  "description": "The project we need but we don&#39;t know it yet.",
  "author": "llabourbeExcilys",
  "private": true,
  "scripts": {
    "dev": "nuxt-ts",
    "build": "nuxt-ts build",
    "generate": "nuxt-ts generate",
    "start": "nuxt-ts start"
  },
  "dependencies": {
    "@nuxt/typescript-runtime": "^0.4.0",
    "axios": "^0.19.2",
    "drag-controls": "^1.0.4",
    "nuxt": "^2.0.0",
    "three": "^0.115.0"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^0.6.0",
    "@nuxtjs/vuetify": "^1.0.0"
  }
}
